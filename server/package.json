{
    "dependencies": {
        "@imagekit/nodejs": "^7.5.0",
        "axios": "^1.18.1",
        "bcryptjs": "^3.0.3",
        "cookie-parser": "^1.4.7",
        "cors": "^2.8.6",
        "dotenv": "^17.4.2",
        "express": "^5.2.1",
        "express-rate-limit": "^8.6.0",
        "express-validator": "^7.3.2",
        "googleapis": "^173.0.0",
        "ioredis": "^5.10.1",
        "jsonwebtoken": "^9.0.3",
        "mongoose": "^9.6.1",
        "morgan": "^1.10.1",
        "multer": "^2.1.1",
        "nodemailer": "^9.0.3",
        "passport": "^0.7.0",
        "passport-google-oauth20": "^2.0.0",
        "rate-limit-redis": "^6.0.0",
        "razorpay": "^2.9.8",
        "resend": "^6.16.0"
    },
    "name": "server",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "vitest run",
        "test:watch": "vitest",
        "dev": "npx nodemon server.js",
        "seed": "node src/scripts/seed.js",
        "build": "node scripts/sync-client-build.js",
        "test:load:prepare": "node scripts/generate-load-tokens.js",
        "test:load": "npm run test:load:prepare && mkdir -p tests/reports && npx -y artillery run --output tests/reports/load-report-$(date +%Y-%m-%d_%H-%M-%S).json tests/load-test-ecommerce.yml",
        "test:load:sciolto-auth": "npm run test:load:prepare && mkdir -p tests/reports && npx -y artillery run --output tests/reports/sciolto-load-report-auth-$(date +%Y-%m-%d_%H-%M-%S).json tests/sciolto-load-test-auth.yml"
    },
    "devDependencies": {
        "supertest": "^7.0.0",
        "vitest": "^4.1.10"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "type": "module"
}
